name: Create Lane
on: push
  # pull_request:
  #   types: [opened]
  #   branches:    
  #     - main

  
jobs:
  setup_npm:
    name: setup npm

    runs-on: ubuntu-latest

    if: startsWith(github.head_ref, 'components/')

    strategy:
      matrix:
        node-version: [15.x]

    steps:
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

  extract_local_lane_name:
    name: extract local lane name

    runs-on: ubuntu-latest

    env:
      BRANCH_NAME: ${{ github.event.pull_request.head.ref }}

    steps:
      - name: Replace illegal characters from head branch name
        uses: frabert/replace-string-action@v2.0
        with:
          pattern: '\/'
          string: $BRANCH_NAME
          replace-with: '_'
  
  setup_bit:
    name: setup bit

    runs-on: ubuntu-latest

    needs: [setup_npm]  

    env:
      BIT_TOKEN: ${{ secrets.BIT_TOKEN }}

    if: startsWith(github.head_ref, 'components/')
    
    steps:
      - run: echo ${{ steps.extract_local_lane_name.outputs.replaced }}
      - run: echo ${{ steps.setup_bit.outputs.replaced }}
      - run: echo ${{ needs.setup_npm.outputs.replaced }}
      - run: echo ${{ steps }}
      - run: echo ${{ needs }}

      # - uses: actions/checkout@v3
      # - name: Install Bit Version Manager
      #   run: npm i -g @teambit/bvm
      # - name: Install latest Bit version
      #   run: bvm install
      # - name: add bvm bin folder to path
      #   run: echo "$HOME/bin" >> $GITHUB_PATH
      # - name: Set up bit config
      #   run: |
      #     bit config set analytics_reporting false
      #     bit config set anonymous_reporting false
      #     bit config set user.token $BIT_TOKEN
      # - name: Import Components
      #   run: bit import
      
      # - name: Create Local Lane
      #   run: bit lane create ${{ steps.extract_local_lane_name.outputs.replaced }}
      # - name: Install and Compile all Components
      #   run: bit install && bit compile
      # - name: Snap all Components to the Lane
      #   run: bit snap -a
      # - name: Export the Lane
      #   run: bit export
